// Starting dependencies.
var express = require("express");
var bodyParser = require("body-parser");
var path = require("path");

// Setting up express to work properly
var app = express();
var PORT = 7000;